{
  "mcpServers": {
    "openclaw": {
      "command": "${CLAUDE_PLUGIN_ROOT}/.venv/bin/python3",
      "args": ["-m", "openclaw.mcp_server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {}
    }
  }
}
